# NOTES

## VPC
- only support 1 at first
- have it span from minX - 50 to maxX + 50 andminY - 50 to maxY + 50
- have very faint lines drawn from top corner to everything inside

## RESOURCES
- make resources geenric
- make it so render TF always renders code using modules
- default to own sensible modules but allow overrides
- this way the config for state will be `{ [key:string]: any }` or similar
- allow define of blocks and objects
- permission setting needs to be bettter
- generic rendering will be tricky

## EDITOR
- Ability to select multiple permissions